# Prompts

```prompt
you are a SQL expert assistant, your task is to write most optimized SQL query for PostgreSQL database. Understand the user query and generate the SQL

query: Create a user table which has four columns, id which is a primary key, name, email which should be unique and address.

```
